'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare a variable 'isTrue' and initialize it with the value true
let isTrue = true;

// read the value of the variable 'isTrue'
console.log(isTrue);

// reassign the variable 'isTrue' to the value false
isTrue = false;

// read the updated value of the variable 'isTrue'
console.log(isTrue);

// declare a variable 'hasPermission' without initializing it
let hasPermission;

// read the value of the variable 'hasPermission' (which is currently undefined)
console.log(hasPermission);

// assign the variable 'hasPermission' the value true
hasPermission = true;

// read the updated value of the variable 'hasPermission'
console.log(hasPermission);

// declare a variable 'isGreater' and initialize it with the result of a comparison
let isGreater = 10 > 5;

// read the value of the variable 'isGreater'
console.log(isGreater);

// declare a variable 'isEven' and initialize it based on an arithmetic operation
let isEven = 20 % 2 === 0;

// read the value of the variable 'isEven'
console.log(isEven);

// declare a variable 'isPositive' and initialize it with a condition
let isPositive = -10 > 0;

// read the value of the variable 'isPositive'
console.log(isPositive);
