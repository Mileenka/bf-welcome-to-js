'use strict';

/* Null

TODO: create a few variables(null):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare a variable 'isNull' and initialize it with the value null
let isNull = null;

// read variable 'isNull' with the value null
console.log(isNull);

// reassign the variable 'isNull' to a different value (in this case, null remains unchanged)
isNull = null;

// read the updated value of the variable 'isNull' which is null
console.log(isNull);

// declare a variable 'emptyValue' and initialize it with the value null
let emptyValue = null;

// read the value of the variable 'emptyValue' which is null
console.log(emptyValue);
