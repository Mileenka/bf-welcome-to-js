'use strict';

/* Numbers

TODO: create a few variables(numbers):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare variable myNumber
let myNumber;

// initialize variable which value is 20
myNumber = 20;

// read variable myNumber whose value is 20
console.log(myNumber);

// reassigned variable for value 40
myNumber = 40;

// read variable myNumber whose value is 40
console.log(myNumber);

// Additional variables with numbers
let anotherNumber = 10;
let decimalNumber = 3.14;
let negativeNumber = -5;

// Read and print the values of the additional variables
console.log(anotherNumber);
console.log(decimalNumber);
console.log(negativeNumber);